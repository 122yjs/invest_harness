# invest-harness Agent Plugin

This directory is generated from `plugins/vertical-plugins/invest-research/`.

- Edit source files in the vertical plugin.
- Regenerate this layer with `pwsh ./scripts/Sync-InvestSkills.ps1`.
- Do not add real analysis logic to command stubs.
- Runtime output paths must use `${ACTIVE_WORKSPACE}` or a dynamic workspace name.

