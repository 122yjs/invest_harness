# 역할 확장 메모

현재 안정적으로 재사용할 역할은 `.agents/skills/` 아래의 스킬로 정의한다.

이 폴더에는 다음 경우에만 별도 역할 문서를 추가한다.

- 스킬로 만들기에는 좁지만 반복적으로 참조해야 하는 역할 브리프가 있을 때
- 특정 산업, 국가, 회계 기준별 보조 역할이 필요할 때
- 모델별 임시 회복 규칙처럼 나중에 제거 가능한 지침을 스킬 본문과 분리해야 할 때
